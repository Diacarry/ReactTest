import React from 'react';
import ReactDOM from 'react-dom';

const element = <h2>Hello, this is the clase2</h2>;

const contenedor = document.getElementById('root');

ReactDOM.render(element,contenedor);